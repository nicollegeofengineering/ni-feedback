export const metadata = {
  title: 'Admin Panel | NICET Feedback',
  description: 'Admin dashboard for NICET Feedback System',
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}